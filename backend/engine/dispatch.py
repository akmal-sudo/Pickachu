"""
Phase 3 — Fleet dispatch (NOT YET IMPLEMENTED).

Will contain a DispatchEngine that takes vehicle/victim coordinates, builds
a cost matrix via RoutingEngine.shortest_path(), and runs the Hungarian
algorithm (scipy.optimize.linear_sum_assignment) for optimal assignment.
Built when we start Phase 3 — see docs/phase-plan.md.
"""
